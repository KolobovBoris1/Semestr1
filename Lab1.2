#include <iostream>
#include <cmath>

using namespace std;

iint main() {
    setlocale(LC_ALL, "Russian");
    int n,m,k;
    cout<<"write n: ";
    cin>>n;
    cout<<"write m: ";
    cin>>m;
    k = n---m;
    cout<<"answer 1 (n---m): "<<k<<endl;
    if (m--<n)
        cout<<"answer 2 (m--<n): true "<<endl;
    else
        cout<<"answer 2 (m--<n): false"<<endl;
    if (n++>m)
        cout<<"answer 3 (n++>m): true "<<endl;
    else
        cout<<"answer 3 (n++>m): false"<<endl;
    return 0;
}
